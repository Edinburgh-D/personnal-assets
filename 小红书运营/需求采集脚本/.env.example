# 小红书 Cookie 配置（必须）
# 获取方式：
# 1. 浏览器登录 www.xiaohongshu.com
# 2. 按 F12 → Network → 刷新页面 → 点击任意请求
# 3. 在 Request Headers 中找到 cookie 字段
# 4. 复制完整字符串（包含所有字段）
# 5. 粘贴到下方单引号内

COOKIES='粘贴你的完整 Cookie 字符串到这里'

# 可选：代理配置（降低封号风险）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# 注意事项：
# - Cookie 时效约 1-7 天，失效后重新获取
# - 建议固定一台设备登录，不要频繁切换
# - 采集频率建议每天不超过 2 次
